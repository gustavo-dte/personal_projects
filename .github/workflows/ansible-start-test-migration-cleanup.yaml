name: Ansible-Start-Test-Migration-Cleanup

# Cleans up test migration VMs created by Ansible-Start-Test-Migration.
# Calls: ansible/roles/python_scripts/read_manifest_value.py to read subscription ID from manifest
# Calls: ansible/playbooks/start-test-migration-cleanup.yml

on:
  workflow_dispatch:
    inputs:
      manifest:
        description: "Manifest name (directory in ansible/vars/)"
        required: true
        type: string

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  start-test-migration-cleanup:
    environment: dev
    runs-on:
      - self-hosted
      - linux
      - non-prod
      - vm-migrate
    env:
      ANSIBLE_ROLES_PATH: ansible/roles
      ANSIBLE_CONFIG: ansible/ansible.cfg

    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v4

      - name: "Install PyYAML for manifest parsing"
        run: pip install PyYAML

      - name: "Validate manifest"
        uses: ./.github/actions/validate-manifest
        with:
          manifest: ${{ inputs.manifest }}

      - name: "Read subscription ID from manifest"
        id: manifest_sub
        env:
          MANIFEST: ${{ inputs.manifest }}
          MANIFEST_KEY: target_subscription_id
        run: |
          SUB_ID=$(python3 ansible/roles/python_scripts/read_manifest_value.py)
          echo "subscription_id=${SUB_ID}" >> $GITHUB_OUTPUT
          echo "[INFO] Target subscription: ${SUB_ID}"

      - name: "Azure Login"
        uses: azure/login@v2
        with:
          auth-type: IDENTITY
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          client-id: ${{ secrets.VM_MIGRATE_MI_CLIENT_ID }}
          subscription-id: ${{ steps.manifest_sub.outputs.subscription_id }}
          enable-AzPSSession: true

      - name: "Display configuration"
        run: |
          echo "[INFO] Manifest : ${{ inputs.manifest }}"
          echo "[INFO] This will remove test VMs and associated resources created during test migration"

      - name: "Prepare extra vars"
        run: |
          cat > ansible_extra_vars.json <<JSON
          {
            "manifest": "${{ inputs.manifest }}",
            "migration_action": "test_migration_cleanup"
          }
          JSON

      - name: "Run playbook - start test migration cleanup"
        run: |
          ansible-playbook ansible/playbooks/start-test-migration-cleanup.yml \
            -e @ansible_extra_vars.json
          rc=$?
          rm -f ansible_extra_vars.json
          exit $rc
