name: Ansible - Join Windows Domain

on:
  workflow_dispatch:
    inputs:
      manifest:
        description: "Manifest name (directory under ansible/vars/)"
        required: true
        type: string
      dry_run:
        description: "Dry run — no changes will be made"
        required: false
        type: boolean
        default: true
      force_rejoin:
        description: "Disjoin then rejoin (DESTRUCTIVE — use for stale domain trust)"
        required: false
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  join-windows-domain:
    environment: dev
    runs-on:
      - self-hosted
      - linux
      - non-prod
      - vm-migrate
    env:
      ANSIBLE_ROLES_PATH: ansible/roles
      ANSIBLE_CONFIG: ansible/ansible.cfg
      DELINEA_BASE_URL: ${{ vars.DELINEA_BASE_URL || 'https://dte.secretservercloud.com' }}
      DELINEA_USERNAME: ${{ secrets.DELINEA_USERNAME }}
      DELINEA_PASSWORD: ${{ secrets.DELINEA_PASSWORD }}
      DELINEA_ACCOUNT: SE-Admin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python dependencies
        run: pip install -r requirements.txt
        shell: bash

      - name: Install Ansible collections
        run: ansible-galaxy collection install -r ansible/requirements.yml
        shell: bash

      - name: Validate manifest
        uses: ./.github/actions/validate-manifest
        with:
          manifest: ${{ inputs.manifest }}

      - name: Azure login
        if: ${{ !inputs.dry_run }}
        uses: azure/login@v2
        with:
          auth-type: IDENTITY
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          client-id: ${{ secrets.CLIENT_ID_VM_MIGRATE_DEV_CU_MI }}
          allow-no-subscriptions: true
          enable-AzPSSession: true

      - name: Resolve Delinea SE-Admin passwords
        shell: bash
        run: |
          # GITHUB_MASKS is a temp file the Python script writes env var names into.
          # The next step reads it and emits ::add-mask:: so the runner redacts those
          # values from all subsequent log output. The Python script itself never
          # prints sensitive values, which keeps CodeQL's taint analysis clean.
          MASKS_FILE="$(mktemp)"
          echo "GITHUB_MASKS=${MASKS_FILE}" >> "$GITHUB_ENV"
          MANIFEST="${{ inputs.manifest }}" GITHUB_MASKS="${MASKS_FILE}" \
            python3 ansible/roles/python_scripts/resolve_delinea_secret_id.py

      - name: Mask resolved passwords in runner logs
        shell: bash
        run: |
          if [ -z "${GITHUB_MASKS:-}" ] || [ ! -f "$GITHUB_MASKS" ]; then
            exit 0
          fi
          # Source GITHUB_ENV so resolved env vars are available in this shell,
          # then emit ::add-mask:: for each var name recorded by the Python script.
          while IFS= read -r var_name; do
            [ -z "$var_name" ] && continue
            # Read the value from the env file written by the previous step.
            value=$(grep -m1 "^${var_name}=" "$GITHUB_ENV" | cut -d= -f2- || true)
            [ -n "$value" ] && echo "::add-mask::${value}"
          done < "$GITHUB_MASKS"
          rm -f "$GITHUB_MASKS"

      - name: Build Ansible extra vars
        shell: bash
        run: python3 ansible/roles/python_scripts/build_extra_vars.py
        env:
          MANIFEST_INPUT: ${{ inputs.manifest }}
          DRY_RUN_INPUT: ${{ inputs.dry_run }}
          FORCE_REJOIN_INPUT: ${{ inputs.force_rejoin }}
          DOMAIN_ADMIN_VALUE_INPUT: ${{ secrets.DOMAIN_ADMIN_PASSWORD }}
          DOMAIN_OU_PATH_INPUT: ${{ secrets.DOMAIN_OU_PATH }}

      - name: Run Ansible playbook
        shell: bash
        run: |
          mkdir -p ansible/logs
          ansible-playbook ansible/playbooks/join-windows-domain.yaml \
            -e @ansible_extra_vars.json 2>&1 | tee ansible/logs/ansible-run.log
          rc=${PIPESTATUS[0]}
          rm -f ansible_extra_vars.json
          exit $rc

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: domain-join-logs
          path: ansible/logs/
          if-no-files-found: warn
          retention-days: 90
