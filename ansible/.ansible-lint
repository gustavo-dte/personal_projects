exclude_paths:
  - .cache/
  - .github/
  - roles/**/templates/**  # allow long lines in templates
skip_list:
  - yaml[line-length]
  - command-instead-of-module # azure migrate requires cli commands
