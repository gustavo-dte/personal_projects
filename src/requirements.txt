# Azure Functions and Service Bus dependencies (Updated for Python 3.10)
azure-functions>=1.18.0,<2.0.0
azure-servicebus>=7.12.0,<8.0.0

# Core Azure libraries
azure-core>=1.28.0,<2.0.0
azure-identity>=1.15.0,<2.0.0

# Configuration and validation with Pydantic v2 (Python 3.10 compatible)
pydantic>=2.0.0,<3.0.0
pydantic-core>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Monitoring and telemetry
azure-monitor-opentelemetry>=1.2.0,<2.0.0

# Standard library typing extensions (for Python 3.10 compatibility)
typing-extensions>=4.8.0,<5.0.0

# Development and testing dependencies (Python 3.10 compatible)
pytest>=7.4.0,<8.0.0
pytest-mock>=3.12.0,<4.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-cov>=4.1.0,<5.0.0

# Code quality tools (Python 3.10 compatible)
ruff>=0.1.0,<1.0.0
mypy>=1.8.0,<2.0.0

# Security fixes for known vulnerabilities (Python 3.10 compatible)
# setuptools: Fix for CVE-2022-40897, CVE-2025-47273, CVE-2024-6345
setuptools>=65.0.0,<70.0.0
# black: Fix for CVE-2024-21503
black>=23.0.0,<25.0.0

# Security scanning tools (Python 3.10 compatible)
bandit[toml]>=1.7.0,<2.0.0
safety>=2.3.0,<4.0.0
cryptography==43.0.3
